//
// 由 `scripts/figma_tokens_to_swift.py` 从 Figma Variables 导出生成，请勿手改。
// 源文件: Mode 1.tokens.json
// 重新生成: `python3 scripts/figma_tokens_to_swift.py`
//

import SwiftUI

/// 与 Figma `Color.*` 变量路径一一对应（Mode 1）。
enum TokenColors {
    /// `Color.Background.2.Default` → #1A1A1A
    static let background2 = Color(hex: 0x1A1A1A)

    /// `Color.Background.3.Default` → #1F1F1F
    static let background3 = Color(hex: 0x1F1F1F)

    /// `Color.Background.4.Default` → #292929
    static let background4 = Color(hex: 0x292929)

    /// `Color.Background.5.Default` → #333333
    static let background5 = Color(hex: 0x333333)

    /// `Color.Background.6.Default` → #3D3D3D
    static let background6 = Color(hex: 0x3D3D3D)

    /// `Color.Foreground.Highlight.Default` → #C4F353
    static let foregroundHighlight = Color(hex: 0xC4F353)

    /// `Color.Text.Highlight.Default` → #C4F353
    static let textHighlight = Color(hex: 0xC4F353)

    /// `Color.Keys.Brand.90` → #B2DD4D
    static let basicPrimary = Color(hex: 0xB2DD4D)

    /// `Color.Foreground.DND.Default` → #8F7DFF
    static let foregroundDND = Color(hex: 0x8F7DFF)

    /// `Color.Keys.Purple.60` → #6055A3
    static let keysPurple60 = Color(hex: 0x6055A3)

    /// `Color.Keys.Brand.50` → #6E8636
    static let keysBrand50 = Color(hex: 0x6E8636)

    /// `Color.Keys.Brand.20` → #3B4525
    static let keysBrand20 = Color(hex: 0x3B4525)

    /// `Color.Keys.Brand.90` → #B2DD4D
    static let keysBrand90 = Color(hex: 0xB2DD4D)

    /// `Color.Text.White.Important` → #FFFFFF
    static let textWhiteImportant = Color(hex: 0xFFFFFF)

    /// `Color.Text.White.Primary` → #E0E0E0
    static let textWhitePrimary = Color(hex: 0xE0E0E0)

    /// `Color.Text.White.Secondary` → #B3B3B3
    static let textWhiteSecondary = Color(hex: 0xB3B3B3)

    /// `Color.Text.White.Tertiary` → #808080
    static let textWhiteTertiary = Color(hex: 0x808080)

    /// `Color.Text.White.Disable` → #4D4D4D
    static let textWhiteDisable = Color(hex: 0x4D4D4D)

    /// `Color.Text.Black.Primary` → #000000
    static let textBlackPrimary = Color(hex: 0x000000)

    /// `Color.Text.Black On Color.Primary` → #2B3020
    static let textBlackOnColorPrimary = Color(hex: 0x2B3020)

    /// `Color.Outline.2.Default` → #3D3D3D
    static let outline2 = Color(hex: 0x3D3D3D)

    /// `Color.Foreground.Consequentail.Default` → #FF6C67
    static let foregroundConsequential = Color(hex: 0xFF6C67)

    /// `Color.Keys.Neutral.Black` → #000000
    static let keysNeutralBlack = Color(hex: 0x000000)

    /// `Color.Keys.Neutral.5` → #0D0D0D
    static let keysNeutral5 = Color(hex: 0x0D0D0D)

    /// `Color.Foreground.DND.Default` → #8F7DFF
    static let chatFrameStroke = Color(hex: 0x8F7DFF)

    /// `Color.Keys.Blue.100` → #67B3FF
    static let pushToTalkBorder = Color(hex: 0x67B3FF)

    /// `Color.Keys.Neutral.10` → #1A1A1A
    static let pushToTalkFillBase = Color(hex: 0x1A1A1A)

}

